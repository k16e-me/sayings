---
const { bg, class: classes, ...props } = Astro.props
---
<div
    { ...props }
    class:list={[
        classes,
        `w-full rounded-xl flex items-end justify-end p-5 mt-5 overflow-clip`
    ]}
    style={ `background-color: #${bg};` }>
    <p
        class="font-display text-fuchsia-600 -mb-2"
        data-gsap="scale-in-up">
        Silence is golden
    </p>
</div>