---
const { bg, class: classes, ...props } = Astro.props
---
<div
    { ...props }
    class:list={[
        classes,
        `w-full rounded-xl flex items-end justify-end p-5 mt-auto overflow-clip r-image-dim`
    ]}
    style={ `background-color: #${bg};` }>
    <p
        class="font-display text-accent-600 -mb-2"
        data-gsap="silence">
        Silence is golden
    </p>
</div>