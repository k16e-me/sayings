---
import { renderRichText } from '@storyblok/astro'
import ThePiece from '../components/ThePiece.astro'

const { blok } = Astro.props
---
<ThePiece
    title={ blok.title }
    content={ renderRichText(blok.piece) }
/>