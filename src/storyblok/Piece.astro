---
import { renderRichText } from '@storyblok/astro'
import PieceComponent from '../components/Piece.astro'

const { blok } = Astro.props
---
<PieceComponent
    title={ blok.title }
    content={ renderRichText(blok.piece) }
/>